/**
 * Usage: "Ich muss" | "Das andere Auto muss" + direction + "."
 */
export enum DriveDirections {
	NONE = "stehen bleiben", // = STAY, not used except as default
	FORWARD = "geradeaus",
	LEFT = "nach links",
	RIGHT = "nach rechts"
}
